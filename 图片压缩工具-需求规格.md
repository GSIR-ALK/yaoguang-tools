# 图片压缩与格式转换工具 · 需求规格

## 一、基本信息

| 项目 | 内容 |
|:---|:---|
| 工具名 | 图片压缩与格式转换器 |
| URL | `/tools/image/` |
| 品牌 | 摇光工具库 |
| 技术栈 | 单 HTML 文件，零依赖，纯浏览器端 |

---

## 二、核心功能

### F1. 拖拽/点击上传
- 支持拖拽图片到页面，或点击上传区域选择文件
- 支持批量上传（多个文件同时处理）
- 支持格式：PNG、JPG、WebP、BMP、SVG（SVG仅格式转换，不压缩）
- 单文件上限 50MB，超限弹窗提示
- 上传后显示缩略图列表（文件名、原始大小）

### F2. 压缩设置
- 质量滑块：1%–100%（默认 80%）
- 预设快捷按钮：无损(100%) / 高清(90%) / 均衡(75%) / 极限(50%)
- 最大宽度/高度限制（可选，保持原始比例缩放）
- 实时预览压缩后大小（每张图显示压缩比）

### F3. 格式转换
- 输出格式下拉框：PNG / JPG / WebP
- 格式特性提示：如「WebP 体积最小但部分老设备不兼容」
- 可批量统一格式，也可逐张选择不同格式

### F4. 批量下载
- 单个下载按钮 / 一键打包下载 ZIP
- 下载前显示压缩结果：原大小 → 压缩后大小 → 节省比例
- 文件命名：`原文件名_compressed.xxx`

### F5. 隐私保证
- 页脚：「摇光工具库 · 纯浏览器端处理 · 图片不上传服务器」

---

## 三、UI 布局

```
┌──────────────────────────────────┐
│  ◇ 摇光工具库         ☀️ 暗色模式 │  ← 复用二维码 CSS
├──────────────────────────────────┤
│                                  │
│  ┌────────────────────────────┐  │
│  │   📁 拖拽图片到此处        │  │  ← 上传区（虚线框）
│  │   或点击选择文件            │  │
│  │   支持 PNG/JPG/WebP/BMP    │  │
│  └────────────────────────────┘  │
│                                  │
│  已上传：3 个文件                 │
│  ┌─ 🖼️ photo1.jpg  2.1MB ────┐  │
│  ┌─ 🖼️ photo2.png  5.3MB ────┐  │  ← 缩略图列表
│  ┌─ 🖼️ photo3.jpg  0.8MB ────┐  │
│                                  │
│  质量：████████░░  80%           │  ← 压缩设置
│  [无损] [高清] [均衡] [极限]      │
│                                  │
│  输出格式：[ WebP ▼ ]            │
│  最大宽度：[ 1920 ] px           │
│                                  │
│  ┌────────────────────────────┐  │
│  │   photo1.jpg               │  │
│  │   原图 2.1MB → 压缩 420KB  │  │  ← 结果区域
│  │   节省 80% ✅               │  │
│  └────────────────────────────┘  │
│                                  │
│  [💾 全部下载] [📦 打包ZIP]      │
│                                  │
├──────────────────────────────────┤
│  摇光工具库 · 纯浏览器端 · 不上传 │  ← footer
└──────────────────────────────────┘
```

---

## 四、技术要求

| 要求 | 说明 |
|:---|:---|
| **零后端** | Canvas API + FileReader 完成所有处理 |
| **暗色模式** | 复用二维码的 CSS 变量体系 |
| **响应式** | 手机端上传区全宽，结果卡片堆叠 |
| **性能** | 大图用 requestAnimationFrame，超 10MB 提示「处理中」 |
| **ZIP** | 使用 JSZip CDN 打包，或纯前端简易 ZIP |
| **统一设计** | 复用二维码的 header/footer/CSS 变量 |

---

## 五、核心算法

```javascript
// 压缩流程
1. FileReader.readAsDataURL(file)     →  读取图片
2. new Image(); img.src = dataUrl     →  解码
3. canvas = document.createElement('canvas')
4. canvas.width = 目标宽度; canvas.height = 按比例
5. ctx.drawImage(img, 0, 0, w, h)    →  绘制
6. canvas.toBlob(fn, 'image/webp', 0.8) →  输出 Blob
7. blob.size vs file.size             →  显示压缩比
```

---

## 六、部署

```
/var/www/jhbxs.xyz/frontend/tools/image/index.html
```

访问地址：`https://www.jhbxs.xyz/tools/image/`

---

> 摇光工具库 · 第二款工具
